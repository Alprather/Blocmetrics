<h1>Register New Application</h1>

 <div class="row">
   <div class="col-md-4">
     <p>Guidelines for registering new applications:</p>
     <ul>
       <li>Include a name for the application</li>
       <li>Application must be a valid web address</li>
     </ul>
   </div>
   <div class="col-md-8">
     <%= form_for @registered_application do |f| %>
       <div class="form-group">
         <%= f.label :name %>
         <%= f.text_field :name, class: 'form-control', placeholder: "Enter application name" %>
       </div>
       <div class="form-group">
         <%= f.label :url %>
         <%= f.text_area :url, class: 'form-control', placeholder: "Enter application url" %>
       </div>
       <%= f.submit "Save", class: 'btn btn-success' %>
     <% end %>
   </div>
 </div>
